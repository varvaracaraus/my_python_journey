# Flight
# This script prompts the user to enter departure and arrival cities, then prints out the flight route.

# Get the departure and arrival city names
departure_city = input('Enter the departure city: ')
arrival_city = input('Enter the arrival city: ')

# Print the flight route
print(departure_city, '-', arrival_city)
